// allUsers allTeams

import { lastChangeTeamAction, queryClient } from "../config/queryClient.config";
import { teamKeys } from "./teamController";
import { userKeys } from "./userController";

export const undoLastAction = (key: string[], action:string,id:number) => {
    
  if(action=="create"){
    
  }
  
  
};
